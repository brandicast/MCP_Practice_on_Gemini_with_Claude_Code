# ========== Google Gemini API Configuration ==========
# Required: Your Google Gemini API key
# Get your API key from: https://aistudio.google.com/app/apikey
API_KEY=your-gemini-api-key-here

# ========== MQTT Broker Configuration ==========
# MQTT broker hostname or IP address
MQTT_BROKER_HOST=localhost

# MQTT broker port (default: 1883 for non-TLS, 8883 for TLS)
MQTT_BROKER_PORT=1883

# MQTT authentication credentials (optional, leave empty if not required)
MQTT_USERNAME=your-mqtt-username
MQTT_PASSWORD=your-mqtt-password

# ========== MQTT Topic Configuration ==========
# MQTT topic to subscribe to for IoT device events
# Use '#' wildcard to subscribe to all subtopics
# Examples:
#   - brandon/iot/zwave/philio/event/#  (subscribe to all Philio events)
#   - home/devices/+/status             (subscribe to status of all devices)
#   - smart/+/#                         (subscribe to all topics under smart/)
topic_for_iot=brandon/iot/zwave/philio/event/#

# ========== Application Configuration ==========
# Flask server host (default: 0.0.0.0 for Docker, 127.0.0.1 for local)
FLASK_HOST=0.0.0.0

# Flask server port
FLASK_PORT=5000

# Flask debug mode (set to 1 for development, 0 for production)
FLASK_DEBUG=0
